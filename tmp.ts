import Eris from ".";
const c = new Eris.Client("");
c.on("interactionCreate", (interaction) => {
	if(interaction.type === Eris.Constants.InteractionTypes.APPLICATION_COMMAND) {
		interaction.member
	}
	c.createRole("", {
		po
	})
});
